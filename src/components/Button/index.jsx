import { PrimaryButton, SecondaryButton } from './styles'

export const ButtonPrimary = (props) => <PrimaryButton {...props} />
export const ButtonSecondary = (props) => <SecondaryButton {...props} />
